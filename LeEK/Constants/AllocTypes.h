#pragma once
#include "Datatypes.h"

namespace LeEK
{
	enum AllocType
	{
		THREAD_ALLOC,
		PHYS_ALLOC,
		PLAT_ALLOC,
		STLHOOK_ALLOC,
		BULLET_ALLOC,
		FILESYS_ALLOC,
		RENDERER_ALLOC,
		SHADER_ALLOC,
		TRANSF_ALLOC,
		TEST_ALLOC,
		CONFIG_ALLOC,
		STRING_ALLOC,
		INPUT_ALLOC,
		MESH_ALLOC,
		RESFILE_ALLOC,
		RESLOADER_ALLOC,
		FILEWRITE_ALLOC,
		SOUND_ALLOC,
		FONT_ALLOC,
		FREETYPE_ALLOC,
		DATASTRUCT_ALLOC
	};
}
